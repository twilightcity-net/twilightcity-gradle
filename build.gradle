plugins {
    id "tech.harmonysoft.oss.custom-gradle-dist-plugin" version "1.7"
}

gradleDist {
    gradleVersion = '6.6.1'
    customDistributionVersion = '1.0'
    customDistributionName = 'tc'
}

task clean {
    doLast {
        project.buildDir.deleteDir()
    }
}
